[project]
name = "lumberjack"
version = "0.1.0"
description = "Schema on read based logging"
readme = "README.md"
authors = [
    { name = "Kevin Montoya", email = "me@kmontocam.com" }
]
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["uv_build>=0.11.14,<0.12.0"]
build-backend = "uv_build"
